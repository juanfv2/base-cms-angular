import { FormatItemPipe } from './format-item.pipe';

describe('Pipes/FormatItemPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatItemPipe();
    expect(pipe).toBeTruthy();
  });
});
