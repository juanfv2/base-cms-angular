import { JfFormatItemPipe } from './jf-format-item.pipe';

describe('Pipes/JfFormatItemPipe', () => {
  it('create an instance', () => {
    const pipe = new JfFormatItemPipe();
    expect(pipe).toBeTruthy();
  });
});
