import { JfListFilterPipe } from './jf-list-filter.pipe';

describe('JfListFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new JfListFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
