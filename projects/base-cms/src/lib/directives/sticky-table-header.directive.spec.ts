import {StickyTableHeaderDirective} from './sticky-table-header.directive'

describe('StickyHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new StickyTableHeaderDirective()
    expect(directive).toBeTruthy()
  })
})
