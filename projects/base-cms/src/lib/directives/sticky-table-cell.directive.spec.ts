import { StickyTableCellDirective } from './sticky-table-cell.directive';

describe('StickyDirective', () => {
  it('should create an instance', () => {
    const directive = new StickyTableCellDirective();
    expect(directive).toBeTruthy();
  });
});
