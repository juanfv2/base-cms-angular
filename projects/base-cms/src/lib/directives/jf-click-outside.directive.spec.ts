import { JfClickOutsideDirective } from './jf-click-outside.directive';

describe('JfClickOutsideDirective', () => {
  it('should create an instance', () => {
    const directive = new JfClickOutsideDirective();
    expect(directive).toBeTruthy();
  });
});
