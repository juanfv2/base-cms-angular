import { StickyTableDirective } from './sticky-table.directive';

describe('StickyTableDirective', () => {
  it('should create an instance', () => {
    const directive = new StickyTableDirective();
    expect(directive).toBeTruthy();
  });
});
