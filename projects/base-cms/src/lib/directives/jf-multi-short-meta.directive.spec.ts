import {JfMultiSortMetaDirective} from './jf-multi-short-meta.directive'

describe('JfMultiShortMetaDirective', () => {
  it('should create an instance', () => {
    const directive = new JfMultiSortMetaDirective()
    expect(directive).toBeTruthy()
  })
})
