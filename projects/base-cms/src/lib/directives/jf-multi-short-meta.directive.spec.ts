import { JfMultiShortMetaDirective } from './jf-multi-short-meta.directive';

describe('JfMultiShortMetaDirective', () => {
  it('should create an instance', () => {
    const directive = new JfMultiShortMetaDirective();
    expect(directive).toBeTruthy();
  });
});
